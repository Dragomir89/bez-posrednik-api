// const mongoose = require('mongoose');
// const { Schema } = mongoose;

// const constructionType = new Schema({ // старо ново
//     name: {type: String, required: true, unique: true}
// });

// mongoose.model('construction-type', constructionType);